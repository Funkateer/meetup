import React, { Component } from 'react';

class Event extends Component {
  state = {
    events: [],
  }

  render() {
    return (
      // this.state.events
      events
    );
  }
}

export default Event;
